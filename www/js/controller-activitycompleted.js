// Controller for Activity Completed (when the user has finished the activity)
angular.module('app.controllers').controller('activityCompletedCtrl', function($scope, $rootScope) {

            // $scope.distance = $scope.routedistance
                  
            // console.log("elapsed distance: " +  $rootScope.distanceTravelled)

            // console.log("elapsed time taken " + $rootScope.timeTaken )
            // console.log("elapsed avg pace " +  $rootScope.avgPace )
            // console.log("elapsed cal burn " + $rootScope.calburn)

            // $rootScope.distanceTravelled = $scope.distTravelled
            // $rootScope.timeTaken = $scope.time
            // $rootScope.avgPace = $scope.avgpace
            // $rootScope.calburn = $scope.totalcb

})
